table.table.table-striped
  thead
    tr
      th #
      th Reference
      th Department
      th Status
      th Email
      th Staff
      th Reply
  tbody
    - tickets.each_with_index do |ticket, num|
      tr
        th= num + 1
        th= ticket.reference
        th= ticket.department.name
        th= ticket.status.name
        th= mail_to ticket.email
        th= staff_assigned(ticket)
        th= link_to 'reply', new_ticket_reply_path(ticket), remote: true

